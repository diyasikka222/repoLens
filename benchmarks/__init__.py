"""Benchmark scripts for RepoLens.

The scripts in this package are executable directly (``python
benchmarks/<name>.py``) and are also importable so the test suite can reuse
their deterministic generators and report schemas.
"""